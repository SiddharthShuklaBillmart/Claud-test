import { FilterTodosPipePipe } from './filter-todos-pipe-pipe';

describe('FilterTodosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
